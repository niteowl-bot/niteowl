# 🚀 Alpha Launch Readiness

> **Architecture map: `docs/ARCHITECTURE.md`** (2026-08-08) — current architecture, the multi-tenant and calendar picture, and future-compatibility guardrails. Documentation only; nothing was implemented for it. Three findings it raises are tracked as checklist items below: overlapping appointments are not prevented (exact-timestamp capacity check), chat/widget book without consulting the calendar while the phone refuses on it, and a returning customer's second booking overwrites their first.

## 🟢 Calendar availability — a missing FreeBusy calendar now fails closed (PR #5, 2026-08-12, merged `3b2fe47`, LIVE AND PRODUCTION-VERIFIED)
**The last place the availability path failed OPEN.** If Google returned a valid FreeBusy response that **omitted the requested calendar entry**, the absence was read as an empty busy list — and therefore as AVAILABLE. Production logic now fails closed: missing or unreadable calendar data must never be interpreted as free availability.
- [x] Root cause: `parseFreeBusyResponse` returned `entry?.busy ?? []` when the calendar key was absent from Google's `calendars` map. An empty busy list is indistinguishable from "completely free". Nothing about the response looks like a failure — HTTP 200, valid JSON, no `errors` block, just silence about the one calendar we asked about
- [x] It contradicted the rule stated two lines below it, where an `errors` entry deliberately throws because *"treating an unreadable calendar as empty would double-book a customer."* Same rule, one step earlier: **no answer is not "free"**
- [x] Fixed in `src/lib/integrations/providers/google.ts` — one guard. The failure is classified `transient`, **not** `auth_expired`, so a healthy connection is never parked as `needs_reauth` by an incomplete payload. The caller turns it into `lookup_failed` → UNKNOWN → never bookable
- [x] **Intentionally narrow.** One production file, one guard. No refactor
- [x] 12 regression tests (813 → 825) across `tests/googleIntegration.test.mjs` and `tests/calendarReliability.test.mjs` — five at the parser, seven end-to-end through `checkVoiceAvailability`. **Four are controls** proving the fix refuses silence and not free time: an answered-empty calendar still available, a real busy window still a conflict, half-open back-to-back still bookable, no connection parked `needs_reauth`. **Mutation-verified: removing the guard fails 7 of them**, while every control keeps passing in both states
- [x] One existing assertion corrected — it recorded the old behaviour under *"an empty calendar yields no busy windows"*, merging "the calendar answered and is free" with "the calendar never answered". Now split, second half inverted
- [x] **PR #3 false-confirmation protection remains intact** — `mayConfirmBooking` still exactly `isCalendarConfirmed`; verified on `main` after the merge
- [x] **PR #4 leadCapture update-path protection remains intact** — still `backsWithCalendar ? confirmedBooking : …`; verified on `main` after the merge
- [x] **No OAuth, Google Cloud, database/schema, environment, Vapi/Twilio, UI, onboarding or pricing behaviour changed.** Exactly three files landed, +171/−1
- [x] **Production verified (2026-08-12)** — Vercel deployment `Ready`, its `githubCommitSha` matched merge commit `3b2fe4721cb63dcbc456eb15be8395d8db0370c8` exactly, and **`niteowlhq.com` returned HTTP 200**
- [x] ✅ **RESOLVED by PR #9 (2026-08-12, merged `b18354e5050dedbb13e0f5324350980f096794a2`, live and production-verified).** *(Previously listed here as 🔴 OPEN, deliberately not changed by PR #5.)* **The defect, kept for the record:** business hours were evaluated in `Europe/London` rather than the organisation's own timezone — `getLondonParts` accepted a timezone parameter and **neither call site ever passed one**, while appointment instants were parsed in the org's real zone. For a London business the wrong zone is the right answer, which is why it survived. Measured: a New York org asking for **06:00 local — three hours before it opened — was ACCEPTED**, because that instant is 11:00 London and lands inside 09:00–17:00; midday New York was refused for the mirror reason. **The fix:** `getZonedParts` (renamed) now *requires* the zone — the default was the whole defect — and the zone is resolved **once** per decision and threaded down (1 read per decision, 0 when supplied, none per slot). New strict `resolveOrgTimezone` reports whether the org's own valid zone was actually read; availability **fails closed** when it was not, through the existing `internalCheckFailed` / `lookup_failed` channel, so the customer hears "we cannot confirm that time right now" and never the untrue "outside our opening hours". `getOrgTimezone`'s soft fallback is unchanged for everything that merely *speaks* a time. **A pre-push forensic review caught a second defect before it shipped:** the voice tool passed the soft fallback downstream as though resolved, bypassing all three guards — same org, chat said `available=false` while voice said `available=true`. Voice now uses strict resolution and bails. DST and half-hour zones are handled by IANA rules via `Intl`; no manual offset arithmetic. Tenant-isolated — proven by a test asserting the same UTC instant yields different verdicts for two orgs. **+16 tests**, mutation-verified three ways; full suite **841/841**, `tsc` clean
- [x] ✅ **DATABASE half done — `organisations_timezone_shape` LIVE IN PRODUCTION (PR #11, merged `2a2a008017c78634fc493c56d569417b73b33c89`; SQL executed manually 2026-08-12).** *(Previously the first half of a combined ⚠️ item.)* The column was `NOT NULL DEFAULT 'Europe/London'` with **no CHECK**, so `''`, `'   '`, `' Europe/London'` and `'BST'` were all storable — and `'BST'` is the dangerous one, because `Intl` accepts it and silently resolves it to Asia/Dhaka (UTC+6). The constraint is `timezone = btrim(timezone) AND btrim(timezone) <> '' AND (timezone = 'UTC' OR timezone LIKE '%/%')` — mirroring the application's own `IANA_ID_SHAPE` rule rather than freezing a list of zone names, which Postgres cannot keep in step with the runtime's ICU build. **Executed by the owner in the Supabase SQL Editor**, not by tooling: the service-role key authorises data access, not DDL, and there is no DDL path from the development environment. **Production verification:** `pg_constraint` returns the constraint once with `contype = 'c'` and the expected definition; `total_orgs = 2`, `offending_rows = 0`; **no existing organisation data was modified** — adding a CHECK validates rows, it does not rewrite them. Rollback remains one lossless line (`drop constraint if exists`). +4 tests, including one asserting **exhaustively** that all 418 zones the application can canonicalise satisfy the SQL predicate, so ICU drift is caught in CI
- [ ] ⚠️ **APPLICATION half still OPEN — deferred to the timezone-selection UI.** No code path writes `organisations.timezone` today, and `canonicaliseTimezone()` / `listSupportedTimezones()` still have **zero consumers** in `src/lib/calendar/timezone.ts`. When a picker is built, the raw choice must go through `canonicaliseTimezone()`, a `null` return must be treated as a validation failure rather than defaulting, and only the canonical string may be persisted. Wiring it before the UI exists would be code with no caller, so it belongs in the picker's own PR. Until then the sole organisation-creation path (`src/app/onboarding/page.tsx`) omits the column and safely inherits the database default
- [x] **PR #9's runtime protection is unaffected by the hardening.** The constraint is preventative only: `resolveOrgTimezone` still fails closed on an unresolvable zone, availability still refuses rather than substituting `Europe/London`, and no `src/` file was touched by PR #11
- [x] ✅ **RESOLVED by PR #7 (2026-08-12, merged `9313ecb2de62863ab06a54ffc89ea50532243ad1`, live).** *(Previously listed here as 🟡 unfixed, "needs a separate test-reliability task".)* **The defect, kept for the record:** the `describe("a pending appointment request holds the slot")` block in `tests/voiceAvailability.test.mjs` hardcoded **2026-08-12** with fixed afternoon times (14:30, 15:00, 15:30 London), and its assertions failed one by one as the real clock passed each of them — the 14:30 case broke at 13:30Z. Worse than the failures were the *silent passes*: the cases asserting `AVAILABLE` kept passing once their slot fell into the past, proving nothing about overlap at all. **Production behaviour was always correct** — `availabilityTool.ts:97` deliberately treats an already-past slot as held by nothing — and it is unchanged. **The fix:** a deterministic controlled clock via `node:test`'s built-in `mock.timers` (`apis: ["Date"]`, `now` = 2026-08-12T08:00:00Z = 09:00 BST, reset after each test). **No dependency added**; only `Date` is faked, so real timers, `new Date(iso)` parsing and `Intl` Europe/London resolution are all preserved. Merely moving the fixture further into the future was rejected as a longer fuse, not determinism — production still compares against `Date.now()`. Test-only: **1 file**, no production source touched. Verified: focused **39/39**, full suite **825/825**, `tsc --noEmit` clean; mutation-checked by moving the controlled `NOW` past every fixture slot, which fails 10 tests and proves the held-slot path is genuinely exercised
- [ ] ⚠️ Calendar work as a whole is **not** complete — booking creation, cancellation and rescheduling reliability have not yet had the same treatment

## 🟢 `no_calendar` could confirm a booking that did not exist — FIXED AND LIVE (PR #3, 2026-08-12, `7f3b136`, merged `ccc8fe2`)
**Merged and deployed to production 2026-08-12.** *(Superseded status: this section previously read "FIXED, NOT DEPLOYED — committed on branch, not pushed, not merged". That was accurate when written and is corrected here.)* `mayConfirmBooking()` returned true for `no_calendar`, so an **allowlisted** org whose calendar is disconnected — or has sync switched off — got status `booked`, a null `unavailableReason`, *"your appointment IS NOW BOOKED"* in the reply, and a confirmation email, with no event in anyone's Google Calendar.
- [x] Root cause: the exclusion's stated justification ("an org with no connection has nothing to contradict") described a call production never makes. `settleCalendarBacking` is the only caller and is reached exclusively through `requiresCalendarBacking()`, which gates on the same `isCalendarEventCreationEnabled(orgId)`. An org with no calendar integration never reaches the function — **that** path is what preserves the legacy single-write behaviour, not this exclusion
- [x] Fixed in `src/lib/calendarSync.ts` — `mayConfirmBooking` is now exactly `isCalendarConfirmed`. Availability is not a booking
- [x] 13 regression tests: an exhaustive sweep over the `CalendarConfirmOutcome` union so a new outcome cannot default into confirmable, plus end-to-end coverage through the real `capturePartialLead` and real `buildBookingOutcomeNote` proving the refusal reaches the reply the customer sees. Includes a control test so the group cannot pass on a note builder broken to return null. **Mutation-verified: the old one-liner fails 11 of them.** 803 passing, 0 failing; `tsc` clean
- [x] **No voice/phone code, prompt, Vapi configuration, call sequencing, availability or end-call behaviour touched.** `voice/calls.ts` consumes only `leadId` from the capture result, and runs post-call
- [x] **Pre-deploy config check done (2026-08-12).** Only `Niteowl Test` resolves to a writable calendar under the code's exact predicate; `Verification Plumbing Co` has no calendar resource. All `integration_links` in prod belong to the test org
- [ ] ⚠️ **The allowlist value cannot be read back** — `CALENDAR_EVENT_CREATION_ORG_IDS` is typed *Sensitive*, so `env ls` shows `Hidden` and `env pull` returns `[SENSITIVE]`. Line 6 below records it as the test org alone; the creation date and link data corroborate that but do not prove it. **This does not gate the deploy**: if the list is the test org alone the change is a no-op today, and if another org is on it that org is currently getting the bug. To make it *known* rather than inferred, overwrite it deliberately (`vercel env add … --sensitive --force`) and redeploy
- [ ] ⚠️ **The Google token has not refreshed since 2026-08-08** — `last_verified_at`, `updated_at` and `token_expires_at` all sit at that date and a successful refresh writes all three back; with `status=connected` and `last_error` null this reads as *not exercised*, not broken. Unrelated to this fix (a dead token yields `unverified`/`failed`, never `no_calendar`) but it lands alongside the refresh-token expiry noted below
- [x] ✅ **RESOLVED by PR #4 (2026-08-12, `40e1030`, merged `e45ee07`, live).** *(Previously listed here as 🔴 "Owner decision needed"; the decision was taken and the fix is deployed.)* `leadCapture.ts`'s update path returned `booked: confirmedBooking || safeNextStatus === "booked"`, and `backsWithCalendar` implies `safeNextStatus === "booked"` by construction — so the OR could never be false when the calendar was consulted, and `settleCalendarBacking`'s verdict was computed and then discarded. No customer ever saw a false confirmation (`unavailableReason` was also set, and `buildBookingOutcomeNote` checks that first), but the reported value contradicted the persisted row. Now `backsWithCalendar ? confirmedBooking : confirmedBooking || safeNextStatus === "booked"` — the calendar's verdict wins where it spoke, and the OR is deliberately kept where it did not, so an already-`booked` lead still reports `booked` on reschedule. 2 files, +215/−1; 10 regression tests (803 → 813); mutation-verified — restoring the unconditional OR fails 3 of them

## 🟢 Google Calendar sync — LIVE AND PRODUCTION-VERIFIED (2026-08-08, `7fb7b04`, test org only)
**Milestones 5 and 6 are live, scoped to `Niteowl Test` alone**, and verified end to end against a real Google Calendar. `CALENDAR_EVENT_CREATION_ORG_IDS` is set in **Production only** to `e3a9ae40-836a-4a55-a723-8b09a9622050`; Preview and Development are unset, and unset means nobody.
- [x] **Book → create.** Widget booking created exactly one event, one `integration_links` row (`subject_type='appointment'`, `synced`), correct org/connection/resource. Reply stated 14:00; stored `13:00Z` = 14:00 BST
- [x] **Reschedule via the manage link → move.** Same event id, etag changed, no duplicate
- [x] **Reschedule via chat → move.** Same event id, etag changed, no duplicate. This is the path that had the contradiction below
- [x] **Cancel → remove.** Link `deleted`, `last_error` null, lead `cancelled`. **Owner confirmed in Google Calendar that the event is gone** (2026-08-08)
- [x] **Isolation held.** Every link in production belongs to the test org. A booking on the non-allowlisted org produced **zero** links and attempted no external write
- [x] **Timezone correct at every step** — BST (UTC+1) throughout; three reschedules, three matching stored instants
- [ ] ⚠️ **Do not broaden the allowlist without re-running the four-step sequence** for the org being added. The gate is per-org by design; adding an id is the whole rollout mechanism
- [ ] ⚠️ **The 7-day Google refresh-token expiry still applies** while the consent screen is unverified — it will look like a random disconnection. See the OAuth item below

## 🟢 Chat reply contradicted the mutation — FIXED AND LIVE-VERIFIED (2026-08-08, `7fb7b04`)
**Found by the live test above.** A widget reschedule moved the appointment *and* the Google event, while the reply said *"I'm sorry, but I can't change or update appointments… have a team member assist you."* The customer was told the opposite of what happened, and acting on it they would arrive at the old time.
- [x] Root cause: the chat routes run **two** independent model calls, and only **failure** was ever communicated to the one that writes the reply. On success it was told nothing — not that an appointment existed, not that it had moved, not to what time. Production logs confirmed intent was `reschedule`, so BOOKING MODE and the "never say you cannot change a booking" rule were both present: **a negative constraint is not a fact**
- [x] Fixed by `src/lib/bookingOutcome.ts` — the prompt now carries what was actually persisted. Silent unless there is something true to say: no note when refused, not booked, no instant, or an unparseable instant. Only `new_booking`/`reschedule`; `contact_update` deliberately stays silent
- [x] **Live-verified 2026-08-08**: *"Your appointment has been successfully moved to Tuesday, 25 August 2026 at 11:00."* — matching the persisted `2026-08-25T10:00:00Z`, one link, same event id, changed etag, no duplicates
- [ ] Known gap: `contact_update` can silently complete a booking begun in an earlier turn and says nothing. Under-stating, not contradicting — left deliberately

## 🔴 Booking correctness — three findings from the 2026-08-08 architecture review (NOT fixed; owner decision needed)
- [x] ✅ **Overlapping appointments are now prevented (2026-08-08, NOT yet deployed).** `isSlotAvailable` counted booked leads with `.eq("appointment_datetime", …)` — exact equality — so with 60-minute appointments and `max_concurrent_bookings=1`, 10:00 and 10:30 both passed. Capacity is now an interval overlap expressed as a strict range on `appointment_datetime`, sharing one definition (`appointmentOverlapWindow` / `appointmentsOverlap`) with the voice held-slot check so the two cannot drift. Half-open preserved: back-to-back bookings stay legal. **Behaviour change — bookings that used to be accepted are now refused.** 39 new tests; 8 of them fail against the old code, verified by reverting
- [ ] 🔴 **Chat and the widget book without consulting the calendar, while the phone refuses on it.** `capturePartialLead` calls `isWithinBusinessHours`/`isSlotAvailable` directly and never `checkBookingSlot`; the phone path does. With `CALENDAR_AVAILABILITY_BLOCKING` live, a caller is turned away from a slot a website visitor can book straight over — and the website booking is never written to Google, so the phone won't see it either. Same gap in `/api/bookings/manage`, which additionally will desync the external event once writes are wired. **Wire all four paths in one pass.** This is the existing line 61 item, restated with its live consequence
- [ ] 🔴 **A returning customer's second booking overwrites their first.** `findOpenLeadForCapture` layer 2 matches email/phone across conversations with no time bound, and `MERGEABLE_STATUSES` includes `booked` — so a chat/widget customer who books Tuesday and returns a month later to book Friday has the Tuesday row mutated: the first appointment vanishes from the calendar and no confirmation email is sent (the send is guarded by `existing.status !== "booked"`). Voice is unaffected (layer 2 is skipped for `source='voice'`). Confirm real incidence against prod before choosing a fix — prod holds test orgs only
- [x] ✅ **A database read failure no longer confirms an unchecked booking (2026-08-08, NOT yet deployed).** `getBusinessHoursForOrg` returned `[]` for BOTH "no hours configured" and "the query failed", so a Supabase blip read as `no_hours_configured` and produced a confirmed, emailed booking. The two are now distinct: a failed read gives `reason: "lookup_failed"` and fails CLOSED, while a genuinely empty table still fails OPEN — which is the behaviour a business mid-setup depends on. `isSlotAvailable` fails closed too (was `return true`), and `checkSlotCapacity` reports *why*, so Remy says "I couldn't confirm that time" instead of the untrue "that slot is fully booked". `findNextAvailableSlot` returns null rather than a suggestion built on an unread table
- [x] ✅ **Milestone 5 — calendar event creation (2026-08-08, NOT deployed, dark behind a flag).** A validated appointment now creates the Google event via `src/lib/calendarSync.ts`, and the lead's status is decided from the write's outcome: only `created`/`already_linked` reach `booked`; a conflict, an unreadable calendar or a failed write leaves a truthful `needs_review`. Gated behind the new `CALENDAR_EVENT_CREATION_ENABLED` — **unset means off, so deploying changes nothing until it is set**. Both §R2 (idempotency key) and §P1 (`subject_type='appointment'`) decisions were taken as documented. 37 new tests; mutation-verified
- [x] ✅ **Milestone 6 — reschedule and cancel sync (2026-08-08, NOT deployed, same flag).** A reschedule PATCHes the event in place (one event, history kept) and is **refused** if Google will not move it — the local time stays put rather than desyncing. A cancel removes the event but **always succeeds locally**: a customer must never be trapped in an appointment because Google is unreachable, so a failure marks the link `failed` for the owner instead. `stale_link` is gone — an existing link is now realigned by an idempotent update rather than sent for review. Known limit: a move of less than one appointment-duration skips the external check, because free/busy carries no event ids and the appointment would clash with itself. +14 tests; mutation-verified
- [x] ✅ **The idempotency-key contract is fixed (2026-08-08).** The key is `leadId + startMs`, so it identifies this *version* of the appointment. A reschedule can never re-derive the old event's id and be told "already exists" while carrying the wrong hour — the trap below. A test fails if the key is reduced to the lead id
- [ ] ⚠️ *(original note, kept for the record)* **Fix the idempotency-key contract before calendar writes are wired (milestone 5).** `toGoogleEventId(idempotencyKey)` makes the key the *permanent* Google event id, and a 409 returns `alreadyExisted: true`. If the key is derived from the lead id, a **rescheduled** appointment re-derives the same key, hits the existing event and reports success carrying the **old time**. The key must identify this *version* of the appointment, and `alreadyExisted: true` must never be read as "the calendar now matches" without an update or a verification read. Take together with the appointment-identity item below. `docs/ARCHITECTURE.md` §R2
- [ ] ⚠️ **Decide appointment identity BEFORE wiring `createOrgEvent` (milestone 5).** `integration_links` has `unique (subject_type, subject_id, connection_id, capability)`, so whatever is passed as `subject_id` becomes the permanent stored identity of "the thing that has a calendar event" — un-baking it later means a data migration over live rows plus reconciliation against Google. Recommendation: write `subject_type = 'appointment'` from the first event, with the lead's id as the value for now. No new table, no migration, no behaviour change. `docs/ARCHITECTURE.md` §P1

## 🟢 Integrations — choosing a Google Calendar now saves (2026-08-07, deployed `01acbba`, PRODUCTION-VERIFIED)
**The first integration resource ever written in production.** Picking a calendar in Settings → Integrations previously failed every time with *"Could not save your selection."* and reset the dropdown to "Choose…"; `integration_resources` stayed empty while the OAuth connection itself saved fine.
- [x] Root cause — `ON CONFLICT` cannot target a **partial** unique index (`… WHERE staff_id IS NULL`). PostgREST's `onConflict` has no syntax for the predicate, so every attempt was rejected at planning time with `42P10` and surfaced as a 502
- [x] Fixed by matching the index's own key rather than flattening it — clear the previous primary, select on `connection_id + resource_type + external_id + staff_id IS NULL`, then update by id or insert. **No schema, RLS, OAuth, route, UI, environment-variable or feature-flag change**; one function in `src/lib/integrations/connections.ts`
- [x] Deployed — pushed as a single fast-forward commit; the READY production deployment reports `githubCommitSha` `01acbbaa4440…` on `main` and holds the `niteowlhq.com` alias
- [x] **Verified in production (2026-08-07, 16:45 UTC)** — one calendar selection created **exactly one** `integration_resources` row: **`is_primary=true`**, `staff_id=null`, `resource_type=calendar`, `admin@niteowlhq.com`, org `e3a9ae40…`, connection `5cad7b94…`. No duplicate and no orphan row; the table was confirmed empty immediately beforehand, and the row survived an independent re-read
- [x] **Connection unaffected** — remained `status=connected` with `last_error=null`
- [x] **No production error observed** — a live log tail spanning the click, filtered for `42P10` / `Failed to select resource` / `ON CONFLICT` / 5xx, emitted nothing
- [ ] ⚠️ **The selection is stored, but nothing reads it yet** — because `CALENDAR_SYNC_ENABLED` is unset, **not** because the engine is unwired. `checkBookingSlot()` **is** wired into the phone call (`voice/availabilityTool.ts`), so enabling that flag makes Remy consult the calendar on live calls immediately. Chat, the widget and post-call capture remain unwired

## 🟢 Voice — a broad window is not an appointment time (2026-08-06, implemented, NOT yet live-tested)

**Observed on a live call.** Caller: *"I'd like to make an appointment for a burst pipe."* → Remy asked for a day and time → Caller: *"Next Wednesday afternoon."* → Remy resolved **Wednesday, 12 August** correctly, then accepted "afternoon" as the complete appointment time and moved straight on to the caller's name. A usable *callback* preference, but not a schedulable *appointment*.

**Root cause.** Rule 6's window branch said a day plus a window "is enough" unconditionally. That sentence was written for CALLBACKS (rule 13, same day) and was being applied to every intent — Remy did exactly what it said.

- [x] CALLBACK — a broad window ("morning", "afternoon", "evening", "between 2 and 5") still acceptable, kept in the caller's words, never narrowed. Unchanged.
- [x] APPOINTMENT — a window is NOT enough. The date is confirmed, then ONE question: *"Wednesday, 12 August. What time that afternoon would suit you?"* Conversational answers accepted as spoken ("3pm", "around 3", "half three", "quarter past two"); never asked twice.
- [x] Never claims a time is available, free or reserved (rule 9) — no calendar is consulted, and the pre-existing bans on "confirmed"/"booked"/guaranteed slots are untouched.
- [x] Never downgrades an appointment to a callback because it cannot book (rule 13), and rule 13 still bans the reverse.
- [x] Done as a conditional split of the existing rule 6 branch — 3 prompt lines (rules 6, 9, 13), still 13 rules, no new section. `callbackTiming.ts` untouched: a window is still a real timing answer and still reaches `preferred_datetime` verbatim.
- [ ] ⚠️ **Live call to verify** — the only thing that can prove it. Say *"I'd like an appointment for a burst pipe, next Wednesday afternoon"* → expect the date confirmed plus ONE time question. Then say *"next Wednesday at 3 PM"* → expect NO time question at all. Also confirm Remy never says a slot is available or booked.

**Still future work, deliberately not built here:** when live calendar availability is wired up, the appointment branch should offer genuine free slots inside the caller's window and let them choose, instead of asking an open question. The availability engine is now wired into the phone call (`voice/availabilityTool.ts`), though its external-calendar branch is still gated by `CALENDAR_SYNC_ENABLED` — see the calendar section below.

## 🟢 Voice — Remy can now end the call (2026-08-06, deployed `c6046cd`, LIVE-TESTED AND PASSED)
- [x] Vapi's built-in `{ type: "endCall" }` tool added to `model.tools` on both the main and the decline assistant — Remy could previously SAY goodbye but had no way to hang up, so the line stayed open and every further "bye" got answered
- [x] Rule 11: the closing line is said once, nothing after it, and the call ends in the same turn; a caller raising something NEW before the close still keeps the call open
- [x] `endCallPhrases` deliberately NOT used — it matches literal caller speech and would cut off "bye for now, but I have another question"
- [x] **Verified on a real production call** — the line now drops after one farewell
- [x] **Deployment lesson, second occurrence:** the first "failed" live test of this fix was not a code failure — the fix was never committed, so production was still running `cf031ec`. `git show origin/main:<file> | grep <marker>` after pushing is the check that catches it; a green local suite never will

## 🟡 Voice prompt length
- [ ] ⚠️ **15,646 characters, 4,247 over the 11,399 budget.** A lossless compression pass (2026-08-06) recovered only 295 characters before every further cut hit a behaviour pinned by a test — five bolder cuts had to be reverted. Closing the remaining gap means deleting working safeguards. **The 11,399 figure itself has no documented origin**: nothing in code enforces it, no test asserts it, no provider limit corresponds to it, and its earliest appearance already treats it as pre-existing. Decide whether to re-derive the ceiling from measurement or to trade specific behaviours away

## 🟢 Voice — callback handling + aborted-call email (2026-08-06, deployed, NOT yet live-tested)
- [x] Callback vs appointment distinction — prompt rule 13, with one scripted clarifying question and a shorter callback checklist
- [x] "As soon as possible" and other urgency phrases can no longer become a callback date or time — enforced in code (`callbackTiming.ts`), not only in the prompt; the phrase is kept on `leads.metadata.callback_urgency`
- [x] A bare "tomorrow" still requires a time; a day with a broad window ("Thursday afternoon", "between 2 and 5") is explicitly accepted and must not be narrowed; corrections still replace previous values
- [x] A call that ends while still ringing no longer sends the "Remy answered a phone call" owner email; connected calls email exactly as before (`callNeverConnected` in `calls.ts`)
- [ ] ⚠️ **Prompt is 15,047 characters, 3,648 over the 11,399 budget** — HEAD was already 425 over. A trimming pass is needed; nothing was cut in this task because the brief forbade removing working safeguards. Decide before the next live test
- [ ] **Live call to verify the callback flow**: say "I need someone to call me about an appointment", then "as soon as possible" — Remy should mark it urgent and ask for a day or window, and the owner email must not show a date/time of "as soon as possible"
- [ ] **Live call to verify the aborted-ring case**: ring the number and hang up during the silence before Remy speaks — no owner email should arrive, and the call should still appear in the dashboard with its `endedReason`

## 🟡 External calendar integration — milestones 1–3 of 7 (2026-08-04; **no longer inert as of 2026-08-07**)
**Full handover in `SESSION_SUMMARY.md`.** Everything below is behind `INTEGRATIONS_ENABLED` — which **is now set in Vercel production** (verified 2026-08-07), so this section is live rather than dormant. Connecting and selecting a calendar both work in production; see the verified section at the top of this file. What remains inert is the **consumption** side: the phone call *is* wired to `checkBookingSlot()`, but its external-calendar branch is gated by `CALENDAR_SYNC_ENABLED`, which is unset — so nothing reads the selected calendar yet.
- [x] Milestone 1 — schema, credential encryption (AES-256-GCM), provider abstraction (`65b71e4`)
- [x] Milestone 1b — generalised into an Integration Framework every future integration reuses: CRM, WhatsApp, Instagram, SMS, Stripe, Xero, Apple/CalDAV (`b75cc98`)
- [x] Milestone 2 — OAuth connection lifecycle + Settings → Integrations, one generic route pair for all providers (`3473b35`)
- [x] Milestone 3 — availability engine written and tested; **wired into the phone-call path only** (`voice/availabilityTool.ts` → `checkBookingSlot()`), with its external-calendar branch still gated by `CALENDAR_SYNC_ENABLED`. Chat, the widget and post-call capture remain unwired
- [ ] ⚠️ **Complete the dev migration** — the four `integration_*` tables exist on dev but `organisations.timezone` does **not**. One `alter table` needed; exact SQL in `SESSION_SUMMARY.md` §7
- [x] **Migration has run on prod** (`sklcqvvnuigpewzarbiv`) — corrected 2026-08-07: prod **is** reachable from this environment, and `integration_connections` / `integration_resources` were both queried successfully with live rows. The earlier "state unverified / cannot reach prod" note was wrong
- [x] **Credential tables confirmed unreadable via the anon key (2026-08-08)** — checked by exposure rather than by reading `pg_policies`, which PostgREST cannot reach. Same read-only probe against prod with both keys: `integration_connections` returns **1 row to service-role and 0 to anon**, so RLS really is denying the public key on the table holding encrypted Google tokens. `leads` behaves the same (5 vs 0). ⚠️ `integration_jobs` and `integration_links` returned 0/0 — they are simply **empty**, so their deny-all is inferred from the migration, not proven; re-probe once the job queue has run
- [x] Integration environment variables are set in **Vercel production** — `INTEGRATIONS_ENABLED`, `INTEGRATION_TOKEN_ENCRYPTION_KEY`, `GOOGLE_CALENDAR_CLIENT_ID`, `GOOGLE_CALENDAR_CLIENT_SECRET`, `GOOGLE_OAUTH_REDIRECT_URI` all present (names verified 2026-08-07; values are Sensitive/hidden)
- [ ] Cross-check the exact six-variable list in `SESSION_SUMMARY.md` §8, and confirm `.env.local` — **neither was inspected** on 2026-08-07, so only the Vercel production side is evidenced above
- [ ] 🔴 **Start Google OAuth verification** — `calendar.events`/`calendar.readonly` are *sensitive* scopes needing app review that takes days-to-weeks. This gates real customers, not code, and is the longest lead time in the project
- [ ] 🔴 **Publish the Google consent screen before any real business connects** — an app left in *Testing* mode issues refresh tokens that expire after **7 days**, which will look exactly like a random disconnection bug
- [ ] Enable `INTEGRATIONS_ENABLED=true` on **dev only** and test connect → pick calendar → disconnect → reconnect against a real Google account
- [ ] Wire `checkBookingSlot()` into lead capture (chat, the embedded widget, post-call capture) — still not done. The phone call already calls it, so this no longer gates the engine being live
- [ ] Milestones 4–7: enable blocking after validating log-only data, event creation, update/cancel sync, Microsoft
- [x] Milestone 8 (live in-call voice availability) — **built and deployed** (`e1f8ce6` onward): the `check_availability` tool calls `checkBookingSlot()` mid-call, so it is no longer "out of scope and not started". Its external-calendar half still awaits `CALENDAR_SYNC_ENABLED`. Note the voice prompt currently has only ~48 characters of headroom under its 11,399 budget

## 🟢 Voice conversation flow — recap before "anything else?" (2026-08-04, deployed, NOT yet live-tested)
- [x] End-of-call reordered to recap → caller confirms → "anything else?" → goodbye (`990c766`)
- [x] Prompt trimmed back under the 11,399-character budget by removing duplicated wording only — 11,351 as shipped (`e26126a`)
- [x] Five spoken-wording fixes from the live call: business name, email question, half-corrected addresses, duplicated words, "anything else?" asked twice (`860b8d5`)
- [ ] **Live call to verify.** Listen specifically for: (1) whether the business name is still spoken as "Night Owl Test" — if so it is the **TTS voice, not the prompt**, and needs a Vapi pronunciation setting, which was out of scope; (2) whether "anything else?" now comes only once, after the recap

## 🟢 Calendar month view — hidden appointments reachable (2026-08-04, deployed)
- [x] `+N more` was a plain `<p>` with no handler, so appointments beyond the third were unreachable from month view. Now a button opening a popover with the day's full list (`35f736d`)
- [ ] **Click-test in a browser** — verified structurally only (the served HTML contains a real `<button aria-label="Show all 5 appointments">`); `/calendar` is auth-gated and no browser automation was available in-session

## 🟡 Knowledge Base — AI Import (2026-07-16, dev fully verified via real browser use; prod migrated 2026-07-20; real multi-page PDF test 2026-07-20)
- [x] Run the three new SQL files against **dev** (`kioljdihgbcboxlnwghv`) — confirmed applied
- [x] Run the same three SQL files against **prod** (`sklcqvvnuigpewzarbiv`) — done by the owner 2026-07-20 (this environment still can't reach prod directly). **The extend-table file was corrected after a serious trigger bug was found on dev (see CHANGELOG) — the version run on prod was already the corrected one, no separate hotfix needed.** Verified via a consolidated one-shot summary query covering all three scripts: all 4 tables present, RLS enabled on all 4, storage bucket private with its policy present, all 11 new `business_knowledge` columns present, 0 rows with a bad backfill (`status`/`source` defaults applied cleanly to every existing row), both triggers (`business_knowledge_set_audit`, `business_knowledge_revision_trigger`) enabled — owner confirmed all 7 checks `true` via screenshot of the query result.
- [x] Server-side verification against real dev data — FAQ generation, extraction, duplicate detection, audit/revision triggers, storage upload/download all directly tested. Two bugs found and fixed.
- [x] **Owner's own manual pass through the real browser flow, done 2026-07-16**: upload → vision extraction → review/edit → duplicate flagging → Merge resolution → approve → commit → publish → chat preview (both draft-hidden and include-drafts-toggled), all against `Plumbing Co 3` on dev. **Five more real bugs found and fixed along the way** — a serious one (a trigger silently reverting every edit made through the *pre-existing* Knowledge Base UI, not just this new feature), plus a client-state bug, a missing button in-flight guard, a duplicate-matching gap (no stemming), and a validation bug in the approve-a-duplicate flow. Also closed a design gap where an AI-generated FAQ and its sibling KB entry from the same import could silently disagree after only one was edited. Full detail in CHANGELOG. **Also rotated the dev Supabase service-role key** after it was accidentally printed during this session — confirmed dead before rotating, confirmed the new key works after; prod was never exposed.
- [x] Upload a real multi-page PDF (e.g. a scanned menu/price list) — done 2026-07-20: a genuine 4-page PDF (cover + logo image, bordered pricing table, policies, FAQs) uploaded via a real authenticated browser session against `Plumbing Co 3` on dev. All 4 pages processed correctly, table structure and unit conversions ("Approx. 4 hours" → 240 min) extracted accurately, embedded logo correctly ignored as non-content. Two real bugs found and fixed — see CHANGELOG. Still genuinely untested: a real **scanned** (photographed/low-quality) PDF specifically, as opposed to a cleanly-generated one — this run's PDF, while multi-page and real, was digitally generated rather than scanned
- [ ] Confirm a draft entry is invisible on the public widget and to a real inbound voice call specifically (dashboard preview's toggle behavior is fully confirmed; the widget/voice code path uses the same unconditional filter but wasn't separately clicked through with a real widget install or phone call)
- [x] Confirm Merge produces the expected result against a real existing manual entry — verified, no duplicate row created, existing values preserved as designed
- [x] Confirm Replace and Keep-both specifically — done 2026-07-20, both verified against real rows via real browser clicks: Replace fully overwrote the existing entry, Keep-both left the original untouched and inserted a genuinely separate new draft row. See CHANGELOG
- [x] Confirm History → Restore reapplies an older version correctly — done 2026-07-20, real edit → real revision snapshot → real restore verified via a real browser click-through. "Regenerate FAQs" itself (the button, as opposed to its underlying already-tested logic) still wasn't separately clicked through this session
- [ ] Watch actual OpenAI cost for the first few real imports — vision (`gpt-4o`) calls are more expensive than this app's usual `gpt-4o-mini` auxiliary calls; tighten `MAX_FILES_PER_BATCH`/`MAX_PDF_PAGES` in `src/lib/knowledgeImport/constants.ts` if needed
- [ ] Consider rotating the dev Supabase JWT/API keys again before this goes anywhere near prod data, purely as good hygiene after today's exposure incident — not urgent (dev has no real customer data), but worth doing before dev ever holds anything sensitive
- [ ] If a real batch times out mid-processing on Vercel, revisit `maxDuration` in `src/app/api/knowledge/import/[importId]/process/route.ts` (currently 300s, Vercel Pro's ceiling) — this is the first route in the app to need one at all
- [x] **Bug found and fixed 2026-07-20**: a reload/closed tab while a batch was still processing stranded it in the DB with no way back to the review step from the UI — always restarted at "upload." `/knowledge/import` now resumes an in-flight or unreviewed batch automatically. See CHANGELOG
- [x] **Bug found and fixed 2026-07-20**: two Knowledge Base entries sharing the exact same title but different content (e.g. a re-imported "Opening hours" with new times) could silently coexist with no duplicate banner, since content-word dilution masked the title match — a real conflicting-answer risk. Added an exact-title-match short-circuit to the duplicate scorer. See CHANGELOG

## 🔴 Billing (Phase 1 — Stripe, code complete, setup outstanding)
- [x] Run the billing migration SQL against Supabase (2026-07-06 — **correction, same day**: the "single Supabase instance for dev and production" claim below was wrong. The migration had actually only ever been applied to a separate dev/test project (`kioljdihgbcboxlnwghv`) referenced by `.env.local` — real production (`sklcqvvnuigpewzarbiv`) never had it, meaning every widget/dashboard chat request was failing outright. Re-applied directly to the real production project; both existing orgs there grandfathered to `active`. See CHANGELOG for how this was discovered and confirmed.)
- [ ] Add Stripe **test-mode** API keys to `.env.local` (`STRIPE_SECRET_KEY`, `STRIPE_WEBHOOK_SECRET`, `STRIPE_PRICE_ID`) and create a test Product/Price for the flat plan
- [ ] Enable Card + Apple Pay + Google Pay under the Stripe Dashboard's payment methods so Checkout shows all of them
- [ ] Add a test-mode webhook endpoint in Stripe pointing at `/api/webhooks/stripe`, copy the signing secret into `STRIPE_WEBHOOK_SECRET`
- [ ] Verify end-to-end: subscribe via Checkout, confirm `organisations.subscription_status` flips to `active`; cancel via the billing portal, confirm it flips to `canceled` and Remy pauses
- [x] Deployed to production (2026-07-06) — the billing gate is live; existing orgs are unaffected since they're grandfathered `active`, and new orgs get a 14-day trial by default
- [ ] Swap to live Stripe keys + a live Product/Price only once ready to actually charge businesses

## 🔴 Critical (must complete before first business)
- [x] Deploy to production (Vercel + production Supabase)
- [x] Connect a custom domain
- [x] Configure production environment variables — confirmed correct (2026-07-06): `RESEND_FROM_EMAIL` was found still set to the old sandbox address despite being "configured" earlier, and has now been corrected directly in Vercel; verified via a real send
- [x] Run a complete production booking flow against the real production Supabase project (`sklcqvvnuigpewzarbiv`) — the version originally checked off here was mistakenly run against a different dev/test project; redone and confirmed correct (2026-07-06)
- [x] Email booking confirmations — fully verified end-to-end against real production (2026-07-06): correct date stored, both customer and owner emails sent from `remy@mail.niteowlhq.com`, owner copy confirmed `delivered` via Resend's own API
- [x] Customer cancellation/reschedule links (self-service notification emails had the same fire-and-forget issue — fixed alongside booking confirmations)
- [x] Needs-review owner notification emails — fixed (2026-07-08): a message combining a genuine question/complaint with contact details in the same turn was classified as `contact_update`, which skipped the confidence check entirely, so the owner was never notified even though Remy told the customer a team member would follow up; see CHANGELOG
- [x] Owner notification recipient — fixed (2026-07-16): all owner notifications (bookings, needs-review, self-service changes, call summaries) were sent to the org owner's Supabase Auth **login** email via `getOrgOwnerEmail()`, with no way to separate who can sign in from who gets notified — for the pilot org that login email was the owner's personal address, not `admin@niteowlhq.com`. Added a dedicated, owner-configurable `organisations.notification_email` column (preferred when set, falls back to the existing auth-email behaviour otherwise, so every other org is unaffected); set to `admin@niteowlhq.com` on both dev and production for the owner's org(s). `SALES_NOTIFICATION_EMAIL` (separate, marketing-site sales-chat notifications) also updated to `admin@niteowlhq.com` in `.env.local` and Vercel Production, then redeployed. Login and the `ADMIN_EMAIL` access gate for `/admin/sales-leads` deliberately untouched. Verified end-to-end with a real test needs-review notification and a real test booking against the dev org — both confirmed delivered to `admin@niteowlhq.com`, test data cleaned up afterward; see CHANGELOG
- [x] Needs-review escalation email content — fixed (2026-07-16): "Their question" showed whichever message triggered the notification, which for the common pattern (unanswered question → Remy asks for contact details → customer replies with just an email/phone) was the contact-only reply, so the email could display an email address or phone number as "the question." The transcript field existed but was always passed `null`; the escalation reason was only logged, never emailed. Added `buildConversationTranscript()` (last 10 messages) and `resolveEscalationQuestion()` — strips the extracted email/phone from the candidate question text and falls back to "No explicit question captured." when nothing substantive remains — applied at every notification call site; email now also shows a "Why Remy escalated" reason and the conversation transcript. Verified live against three real scenarios (pure contact info, combined question+contact, two-turn question-then-contact-reply) via real sends inspected through Resend's API; test data cleaned up afterward; see CHANGELOG
- [x] Basic monitoring (logs and alerts) — Sentry + `/api/health` shipped 2026-07-04, `NEXT_PUBLIC_SENTRY_DSN` confirmed live in Vercel prod env; external uptime pinger configured against `/api/health` by the owner (2026-07-08) — `/api/health` re-verified responding `200` (`{"status":"ok","database":"ok"}`) at setup time
- [x] **Deleted the stale duplicate `niteowl-app` Vercel project** — done by the owner 2026-08-03. Verified immediately after: `niteowl-app.vercel.app` now returns **404**, while production is untouched (`niteowlhq.com` 200, `/api/health` 200 `{"status":"ok","database":"ok"}`, `/api/voice/incoming` the healthy 401). Identity confirmed before deleting by Project ID — the deleted project was `prj_a6LO1MG2eQA1YdzZTB0iZrJNVBke`, whereas `.vercel/repo.json` links this repo to `prj_zFNQvtQfc8r9zx7AUziiUvgnaJX7` (`niteowl`). Background below, kept for the record.
  - Two Vercel projects deployed from this repo, `niteowl` and `niteowl-app`. **`niteowl` is the only one serving `niteowlhq.com`.**
  - `niteowl-app` is **live but frozen**: its production deployment is Ready and dated **Jul 2** (commit `d522041`, "Add onboarding wizard"), serving `niteowl-app.vercel.app` — a public, month-old copy of the marketing site. Its only domain is that free `.vercel.app` one, so deleting it breaks no DNS. (Its served HTML still carries the default `Create Next App` title, which predates the `metadata` export — an easy thing to mistake for an empty scaffold.)
  - **Every deployment since 2026-07-15 has failed** — 42 of 42, through `3985b76` today — which is why the live build is stuck at Jul 2 and nothing since has shipped there.
  - Verified unreferenced: no `niteowl-app` string anywhere in the repo, no `.github` workflows (no Actions at all), no `vercel.json`, and `.vercel/repo.json` links this working copy to `niteowl` only (`prj_zFNQvtQfc8r9zx7AUziiUvgnaJX7`). DNS has only the apex and `www`, both at `76.76.21.21` and both served by `niteowl`.
  - Verified every production surface points at `niteowl`: domains on the `niteowl` project (CHANGELOG 2026-07-15), `niteowl-pi.vercel.app` serves the real app, `www` 308s to the apex, Supabase Auth redirect URLs → `niteowlhq.com`, Vapi server URL → `niteowlhq.com/api/voice/incoming` (live 401), widget embed → `NEXT_PUBLIC_APP_URL`, uptime pinger → `/api/health` (live 200).
  - **Env vars checked 2026-08-03 — safe to delete, nothing to rotate.** Production holds exactly two variables, both added Jun 27: `NEXT_PUBLIC_SUPABASE_URL` and `NEXT_PUBLIC_SUPABASE_ANON_KEY`. No `SUPABASE_SERVICE_ROLE_KEY`, no Stripe/Resend/OpenAI/Vapi secrets. Both are `NEXT_PUBLIC_*`, i.e. compiled into the browser bundle by design and already public on `niteowlhq.com` — RLS is what protects that data, not secrecy. Added Jun 27 also predates the production Supabase project, so the URL most likely points at dev.
  - **This is also why the builds fail:** the project is frozen with a Jun 27 env-var set, while the app has since come to require `SUPABASE_SERVICE_ROLE_KEY`, `STRIPE_SECRET_KEY`, `RESEND_API_KEY` and `OPENAI_API_KEY` at build time.
  - Domains checked: Production lists `niteowl-app.vercel.app` plus two auto-generated aliases (both behind Vercel Authentication); Preview and Development have no custom domains. No custom domain is possible regardless — DNS holds only the apex and `www`, both served by `niteowl`.
  - Supabase Auth allow-list checked 2026-08-03 — **nothing to clean up**: `niteowl-app.vercel.app` was never allow-listed. `niteowl-production` → Authentication → URL Configuration holds Site URL `https://niteowlhq.com` and two Redirect URLs, `https://niteowl-pi.vercel.app/**` and `https://niteowlhq.com/**`. The `niteowl-pi` entry is deliberately kept — it is the retained `niteowl` project's auto-generated URL, still serving the live app, and a usable sign-in fallback if the custom domain ever has a DNS or cert problem.
  - **Why it matters beyond tidiness:** a public stale copy of the marketing site is indexable and can show prospects month-old copy, pricing and legal pages; and the failing project makes the *combined* GitHub commit status `failure` on **every** commit, green ones included, which will hide a genuine build failure the first time there is one.

## 🔴 Critical infrastructure bugs found and fixed (2026-07-06)
- [x] **Real production was running against a completely different, un-migrated Supabase project than everything tested against all session** — discovered while investigating why sales chat capture failed in production. Confirmed by extracting the real project ref straight out of the production login page's compiled JS. The real project was missing both the billing migration and the new `sales_leads` table; the missing billing columns meant every widget/dashboard chat request was failing at the very first query. Both migrations re-applied to the correct project.
- [x] **Booking confirmation and self-service cancel/reschedule emails were fire-and-forget**, which is unsafe on Vercel's serverless runtime (the function can freeze right after the response is sent, killing unawaited work) — worked fine locally, silently failed in real production. Fixed using Next.js's `after()` across all 4 call sites.
- [x] **Every `resend.emails.send()` call site ignored the SDK's returned `error` field**, which is how the SDK reports API-level failures (it resolves normally instead of throwing) — meaning a real send failure was silently treated as success everywhere. Fixed with a shared `sendChecked()` helper that surfaces the error properly.
- [x] **`RESEND_FROM_EMAIL` in real production was still the Resend sandbox address** (`onboarding@resend.dev`), despite appearing "configured" — it had only ever been added as a sibling of two genuinely new variables, never actually edited itself. This, not a wrong API key or account, was the real reason emails were failing in production. Corrected directly; confirmed via a real delivered send.
- [x] **Relative booking times ("tomorrow at 2pm") could be silently booked on the wrong date** on the public widget (the real customer-facing path) — the widget's lead-extraction prompt had drifted out of sync with the dashboard's and was missing the "return the date exactly as the customer said it" instruction, so the model resolved it itself using a stale internal calendar before the real current date was ever applied. Fixed by resyncing the two prompts; re-verified a real booking now stores the correct date.
- [x] Audited all existing leads (in the correct production project) with a stored appointment time for the same date-corruption pattern — zero affected, no live customer data was corrupted
- [x] Full real-production re-verification after all fixes: booking (correct date + delivered emails) and sales chat demo capture (all 5 fields + delivered notification) both confirmed working end-to-end

## 🔴 Critical infrastructure bug found and fixed (2026-07-20)
- [x] **`RESEND_API_KEY` missing in Vercel Production broke the entire production build, not just email sending** — discovered when the deploy for the 2026-07-20 AI Import fixes failed. `src/lib/email.ts` constructs `new Resend(process.env.RESEND_API_KEY)` at module load time rather than lazily; Next.js's build-time "Collecting page data" step evaluates that module while processing `/api/sales/chat`, and the Resend SDK throws immediately on a missing key — failing the whole build. A local `next build` succeeded (confirming the code itself was fine — `.env.local` has the key) which pointed straight at the Vercel env var. Owner confirmed the var was missing/not scoped to Production, added it (sourced from Resend's dashboard), and redeployed.
- [x] Verified end-to-end against real production after the redeploy: ran a full 6-turn sales-chat demo-lead conversation against `niteowlhq.com` (name → email → phone → company → preferred time → confirmation), all real HTTP calls, no stream errors, and the final reply used the "team will follow up" completion message — which the code only ever sends once `sendSalesLeadNotification` actually succeeds. Owner confirmed the real email ("New sales lead: Claude Code Test — Claude Code Test Co") arrived at `admin@niteowlhq.com`. Test lead row (`conversation_id = claude-code-email-verify-1784579929799`) verified and deleted from production afterward.
- [ ] Not fixed (deliberately, pending owner input): the underlying fragility — `email.ts`'s eager, module-scope Resend client construction — means *any* future misconfiguration of `RESEND_API_KEY` will fail the entire production build again, not just email sending. Owner chose to fix the immediate env var first rather than also changing this working file in the same pass; revisit making the Resend client lazy if this class of failure recurs

## 🟢 Sales chat (marketing site conversion, 2026-07-06)
- [x] Persuasive, outcomes-first sales chat on the landing page, separate persona/system from Remy-as-receptionist
- [x] Objection handling for the 5 most common pushbacks
- [x] Industry personalization (reusable prompt structure, verified against 7 industries including one not explicitly listed)
- [x] Structured, validated, one-field-at-a-time demo lead capture with correction support and cross-session duplicate prevention
- [x] Admin-only `/admin/sales-leads` CRM view + team email notification on completed leads
- [x] Closing CTA — free trial for ready visitors, demo flow for those still deciding
- [x] Fixed (2026-07-07): a returning visitor's permanent `localStorage` conversation id could match an already-completed demo lead, causing Remy to confirm a booking using stale data instead of collecting fresh details — see CHANGELOG. **Superseded (2026-07-08, round 6):** this only excluded *completed* leads; a lead abandoned mid-flow (never confirmed) was still reusable and caused the same class of bug. `localStorage` persistence for the conversation id was removed entirely — see below.
- [x] Fixed (2026-07-07): mobile input font-size (14px) was below iOS Safari's auto-zoom threshold, clipping chat content on focus — see CHANGELOG
- [x] Fixed (2026-07-07): an objection or tangential reply mid-collection could make Remy drop the pending field request entirely; field collection guidance is now an explicit override — see CHANGELOG
- [x] Added (2026-07-07): a demo request now requires explicit visitor confirmation of a full recap before being marked complete or triggering the team notification — see CHANGELOG
- [x] Fixed (2026-07-08): the page behind the chat widget could be scrolled while the widget was open (confirmed via a real device recording, then reproduced precisely: `window.scrollY` moved on a background wheel-scroll with the chat open); now locked and restored correctly on close — see CHANGELOG
- [x] Fixed (2026-07-08, round 3): root cause of mobile message clipping identified and deterministically reproduced/fixed — an interrupted stream (dropped before the server's completion sentinel arrived) left partial text stuck displayed as final, with no error. Now retries once automatically, falls back to a visible error only if that also fails. Verified via forced stream-truncation test, not inferred — see CHANGELOG. Confirmed by the reporter on a real device that this resolved the flow-completion issue.
- [x] Fixed (2026-07-08, round 4): after round 3, the reporter's real Samsung device showed a *different*, more specific issue — the last assistant message rendering partially behind the fixed CTA/composer. Root cause: missing `min-h-0`/`shrink-0` flexbox properties, an engine-dependent overflow bug. Fixed and verified via precise overlap measurement (not just visual screenshot check) across 5 viewport/font-scale configurations including simulated Android "larger text" accessibility settings — see CHANGELOG. Confirmed by the reporter: layout clipping resolved on Chrome for Android.
- [x] Fixed (2026-07-08, round 5): real-device testing after round 4 isolated two remaining **browser-specific** bugs — (1) Chrome for Android's auto-dark-theme left input text nearly invisible (no explicit color/background on the input); (2) Samsung Internet specifically still clipped messages, traced to the completion-sentinel check only inspecting each network chunk in isolation rather than the accumulated text, so a sentinel split across two chunks (plausible given different browsers chunk streams differently) was never detected. Both fixed and deterministically verified (dark-color-scheme emulation for the input; a raw controlled-chunking HTTP server for the sentinel bug, since Playwright's route interception can't simulate real chunk boundaries) — see CHANGELOG. Still pending final confirmation from Samsung Internet specifically (no way to test that engine directly in this environment).
- [x] Fixed (2026-07-08, round 6): **critical** — a fresh-looking chat (empty message list) could silently resume an old, still-open (never-confirmed) lead from a totally different past visit, greeting the visitor by a stale name and referencing a company they never mentioned. Root cause: the conversation id was persisted in `localStorage` indefinitely, but the visible message list never was — always starting empty regardless, masking the fact that the server was continuing an old conversation. Fixed by making the id in-memory-only per page load (no `localStorage` at all), plus proactively clearing the old key for browsers that already had one cached. Reproduced and verified against the dev database with a deliberately seeded stale lead — see CHANGELOG.
- [x] Fixed (2026-07-08, round 7): field extraction could silently drop a message on timeout/error (confirmed via a real production log entry) with no visible error, preventing the booking from ever completing; and the booking could be reported complete even if the team notification failed to send. Extraction now retries and returns a genuine failure signal instead of a false "nothing stated"; notification-sending now happens atomically with the completion transition, so the visitor is only told the booking is complete once the notification actually sent — a failure keeps the lead open and asks for one more confirmation, which naturally retries. Verified directly against the real function (retry) and a real temporarily-broken notification config (gating) — see CHANGELOG.
- [x] Fixed (2026-07-08, round 8): **root cause of the intermittent missing demo notifications** — the field extractor had no conversation context, so bare answers ("Poiu" for company, gibberish names) were sometimes silently dropped, leaving the flow stuck collecting while the reply model claimed the booking was complete; the "Chrome vs Samsung" pattern was coincidence. Extractor now receives the pending field (from lead state) or the previous assistant message (opening name turn). Verified 7/7 E2E runs under both browsers' user-agents plus owner's real-device confirmation on both — see CHANGELOG. **This build is the frozen pilot baseline**; sales-chat diagnostic logging deliberately left live for the pilot.
- [x] Cleaned up (2026-07-08): the five 2026-07-08 production test leads (`priya@brightsmiles.co.uk`, `claude-diag-test@example.com`, `Dean@huy.com`, `Ernie@samsiming.com`, `Lupo@lup.com`) deleted from the real production `sales_leads` table via a temporary, token-gated ops endpoint that matched only these exact ids/emails (verified each row's content matched expectations before deleting, confirmed zero remaining after, then removed the endpoint). No genuine customer leads existed in production at the time.
- [ ] Owner's own manual pass of the authenticated `/admin/sales-leads` view (assistant verified the unauthenticated redirect and the underlying data directly, but not the authenticated render — no access to real login credentials)

## 🟢 Pre-alpha security & reliability audit (2026-07-06)
- [x] Full-codebase audit ahead of external alpha (security, performance, reliability, production-readiness)
- [x] Fixed: unescaped user input in transactional emails (HTML injection into business-owner-facing notification emails)
- [x] Fixed: `/api/chat` lead-capture/knowledge-base access still used an unverified client-supplied `orgId` after a spoofed org lookup returned null (the 2026-07-04 fix only patched the lookup, not the write path)
- [x] Fixed: `/api/chat` had no rate limiting (unlike the other two chat routes) — unbounded OpenAI cost-abuse risk on a no-card-required signup
- [x] Fixed: `/api/bookings/manage` had no rate limiting — a leaked/guessed manage_token could trigger unlimited notification emails
- [x] Fixed: SSRF hardening gap in `/api/widget/verify-install` (DNS-rebinding and redirect-based bypass of the private-IP/localhost check, including the cloud metadata address)
- [ ] `/api/leads` route is dead code (zero callers) with an incomplete status whitelist — flagged, not removed; decide whether to wire it up or delete it

## 🟡 Business onboarding
- [x] Widget installation guide (WordPress, Wix, Squarespace, Shopify, Webflow, HTML, Google Tag Manager) — `Settings → Website Widget`, 2026-07-06, includes a live "verify installation" check
- [ ] Dashboard "Getting Started" guide
- [x] Test the widget on a real website — done 2026-07-20: created a fresh production test org ("Claude Widget Test Co", real signup + email confirmation, no real customer touched) and a genuinely separate public site (GitHub Pages, `niteowl-bot.github.io`) with the real embed snippet. Confirmed `widget.js` loads from `niteowlhq.com`, the page satisfies `/api/widget/verify-install`'s checks, and a real cross-origin `/api/widget/chat` conversation succeeded with correct `Access-Control-Allow-Origin` — Remy correctly deferred rather than inventing an answer for this knowledge-less test org. Production test org and auth user deleted afterward; only one real site tested so far (a static GitHub Pages page), not yet WordPress/Wix/Squarespace/Shopify/Webflow/GTM specifically — those still open
- [x] Verify email deliverability (2026-07-06 — `mail.niteowlhq.com` verified with Resend, `RESEND_FROM_EMAIL` switched to `remy@mail.niteowlhq.com`; confirmed via a real end-to-end send with SPF/DKIM passing and a real booking's confirmation emails checked via Resend's own send log)

## 🟡 Admin
- [ ] Business management view
- [x] Verify backups — enabled by the owner (2026-07-08) on the production Supabase project (`sklcqvvnuigpewzarbiv`) via the dashboard; a Supabase-side setting, no code/config change in this repo
- [ ] Export data capability
- [x] Privacy Policy (2026-07-06 — `/privacy`, tailored to what the app actually collects/processes; not a substitute for a real legal review, but a solid, specific draft rather than generic boilerplate)
- [x] Terms of Service (2026-07-06 — `/terms`, same caveat)
- [x] Linked from the footer (already referenced there), the signup page's existing agreement notice, the sales chat, and the embeddable customer-facing widget (absolute URL, since it renders on third-party sites)

## 🟡 Voice AI (Phase 2, Step 1 — code complete 2026-07-09, dark until setup below is done)
> Owner setup steps below are written up in order with exact values and verification queries in **`docs/VOICE_SETUP_RUNBOOK.md`** (2026-07-10) — follow that document; the items here just track completion.
- [x] Additive voice platform merged: `/api/voice/webhook` + `/api/voice/incoming`, adapter layer (`src/lib/voice/`), durable idempotent event ingestion, lead capture with source `voice` through the existing engine, owner call-summary emails. Kill switch `VOICE_ENABLED` keeps it all 404 until deliberately enabled — deploying is safe with zero production behaviour change
- [x] Run `docs/sql/2026-07-09_voice_tables.sql` in the Supabase SQL editor on the **dev/test project** (`kioljdihgbcboxlnwghv`) — done 2026-07-09, tables verified present
- [x] Run `docs/sql/2026-07-09_leads_source_voice.sql` on the **dev/test project** — done 2026-07-09; constraint confirmed rebuilt with `'voice'` included (dev testing had proved `leads_source_check` really does reject `'voice'`)
- [x] Run BOTH SQL files on **real production** (`sklcqvvnuigpewzarbiv`) — done by the owner 2026-07-10; `leads_source_check` confirmed by the owner to now include `'voice'`
- [x] Complete Step 0 (Vapi account, spend cap, browser-call prototype — see docs/VOICE_AI_PLAN.md) — Vapi account + production number `+18436480204` created and activated 2026-07-10
- [x] In Vapi: set the phone number / assistant server URL to `<app-url>/api/voice/incoming`, set the server-URL secret, and put the same value in `VAPI_WEBHOOK_SECRET` — done 2026-07-10 (`https://niteowlhq.com/api/voice/incoming`), **but see the assistant-assignment item below before calling this configuration correct**
- [x] Insert a `voice_settings` row for the test org (phone_number in E.164, enabled=true) — done and verified by the owner 2026-07-10 (org `e3a9ae40-…2050`, `+18436480204`, enabled)
- [x] Set `VAPI_WEBHOOK_SECRET` + `VOICE_ENABLED=true` in the target environment (Vercel) only after the SQL has run — **done (2026-07-10, late evening)**: after one failed redeploy (env-var misconfiguration, resolved by the owner), both `/api/voice/*` routes externally verified answering the healthy 401 `Invalid secret.` instead of 404
- [x] **Un-assign the dashboard-built "Inbound AI Receptionist" assistant from the Vapi phone number** — confirmed fixed by behaviour (2026-07-10): subsequent real calls hit our server (assistant-request served, events stored, knowledge-gated answers in the transcript)
- [x] Live end-to-end test call — **done (2026-07-10, after two production bugs were found and fixed by real calls)**: two real calls fully processed — events stored + processed with no error, `voice_calls` rows correct, leads created with source `voice` (caller name extracted, dashboard link in the summary emails), owner summary emails delivered. The two bugs the real calls exposed, both fixed + dev-verified + redeployed same evening (see CHANGELOG): (1) Vapi sends decimal `durationSeconds`, which the integer column rejected, killing all processing; (2) Vapi returns `analysis.structuredData` NULL (their documented analysis timeout leaves it empty), so lead extraction now falls back to our own transcript extraction — which is what actually carried both successful calls
- [x] Delete the 2026-07-10 production test rows — **done by the owner (2026-07-12)** via `docs/sql/2026-07-12_voice_test_rows_cleanup.sql`, guided step-by-step with inspection results verified before each delete: 12 voice_events, 4 voice_calls, 1 lead ("James O'Flaherty", deleted by exact id after inspection revealed the merged lead stored the *spoken* local-format phone `0871465274`, not the `+353` caller ID the script's original predicate assumed), 2 conversations. Final verification query confirmed 0 remaining in all four tables; org-wide check confirmed no other `voice` leads existed, so no real customer data was ever in scope
- [ ] **Test call for the lead-completeness fix (2026-08-03)** — a real call must confirm Remy now refuses to hang up on a half-collected lead. Ring the number, report an on-site job, and deliberately answer "tomorrow" when asked about timing: Remy should push for an exact time, ask for the service address, confirm the caller ID ("Is this the best number to reach you on?"), and read everything back before closing. Then check all four surfaces agree — transcript, the email summary's six labels (`Not provided` only where something was genuinely withheld), `leads.preferred_datetime` + `leads.metadata.service_address`, and the dashboard lead's phone (still the caller ID, never a spoken number). Also worth one call where you *refuse* a detail, to confirm Remy accepts the refusal and closes rather than pressing
- [x] **Appointment-request flow verified by real production call (2026-08-03, commit `c2d311b`)** — Remy resolved and confirmed "Thursday, 6 August at 2 PM" out loud, collected and confirmed the name and the email, **asked for the service address unprompted**, asked whether the caller ID was the best callback number, accepted an alternate number and repeated it back to confirm, stored the correct calendar date/time, and included the address and alternate number in the owner notification. Took three live calls to get there, each exposing a real defect: (1) no date confirmation at all — the prompt carried no date, so a spoken weekday could not be resolved; (2) the same call again, which turned out to be running the *previous* commit because the fix had never been pushed — production was still on `0b81e6f`; (3) the address skipped entirely on a boiler service, because email was not a required field and the address/number steps carried "only when…" conditionals the model judged its way out of. See CHANGELOG. **Still untested from the item below:** the vague-time path (deliberately answering "tomorrow") and a call where the caller *refuses* a detail, to confirm Remy accepts the refusal and closes rather than pressing
- [ ] **Watch the summary on that same call** — `analysisPlan.summaryPlan.messages` is the first custom analysis prompt sent to Vapi (shape confirmed against their OpenAPI spec, not assumed). If Vapi rejects it, `call.analysis.summary` arrives **empty** rather than wrong: the email will say "No summary was generated for this call." and the lead still comes from structured data. If that happens, the fallback is to drop the `messages` array back to `summaryPlan: { enabled: true, timeoutSeconds: 30 }`
- [ ] **Follow-up: Vapi's structured data is NULL on every real call so far, even with `timeoutSeconds: 30`** — the transcript fallback covers it fully (it produced the leads on both verified calls), but the provider-side extraction should eventually work or be dropped deliberately; investigate via Vapi call logs/support when time allows (not an alpha blocker)
- [ ] Follow-up (cosmetic): both 2026-07-10 real calls opened with "Hi. For calling. How can I help you today?" — deterministic. Resolved by prod query (2026-07-12): `voice_settings.greeting` is **NULL**, so the code default was in play all along (the earlier "custom greeting" theory was wrong) and the garble is how the call audio/transcript rendered the default — most plausibly the telephony/TTS clipping the opening words. Owner-requested wording change shipped 2026-07-12 ("the AI receptionist" → "your AI receptionist" in `src/lib/voice/assistant.ts`; greeting only, no other voice logic touched). **Listen on the next test call**: if the opening still sounds clipped, the fix is start-of-call audio timing (e.g. a brief leading pause in the greeting), not wording — reopen then
- [x] Verify a phone booking flows through the existing engine — **verified via full dev simulation (2026-07-09)**: simulated end-of-call report → raw event stored + deduplicated → `voice_calls` row with cost → lead created with source `voice` (caller ID as phone, "tomorrow at 2pm" parsed to the correct Europe/London ISO datetime, status `booked` after the availability/capacity checks, `manage_token` issued) → lead linked back to the call → owner booking-confirmation + call-summary emails accepted by Resend (customer copy correctly skipped, phone-only caller). All test rows deleted afterwards; still needs re-confirming with a real Vapi call (item below)
- [x] Confirm whether `leads.source` has a CHECK constraint blocking `'voice'` — **it does** (`leads_source_check`, confirmed via a real dev-project insert failure 2026-07-09); fix is `docs/sql/2026-07-09_leads_source_voice.sql`, tracked above

## ⚪ Version 2
- [x] Voice AI — in progress; moved to its own Phase 2 section above
- [ ] Google Calendar
- [ ] Outlook Calendar
- [ ] Multi-staff scheduling
- [ ] Stripe subscriptions
- [ ] SMS reminders
- [ ] Analytics dashboard

## 🎯 Pilot Customers
- [ ] Identify 10 local businesses
- [ ] Recruit first 3 pilot users
- [ ] Install widget on first live website
- [ ] Complete first real booking
- [ ] Collect feedback
- [ ] Fix reported issues

## ⚪ Known issues (low priority, deferred — not blockers)
- [ ] **Dashboard preview chat swallows the error toast on a failed first message in a brand-new conversation.** `ChatShell.tsx` remounts `ConversationView` (via `key={activeId ?? "empty"}`) the instant a new conversation is created. If the AI call fails on that exact first message, `onError` fires after the remount and sets state on an already-unmounted instance, so no error message renders. The input is never stuck — the freshly-mounted instance starts clean and works immediately — this is a missed notification, not a crash or dead-end. Any later message in an established conversation shows errors correctly. Root cause and full detail in CHANGELOG.md, 2026-07-06 (AI-call reliability bundle). Deliberately not fixed yet — the real fix means revisiting `ChatShell`'s remount-on-conversation-switch strategy, which deserves its own deliberate pass rather than being folded into a reliability bugfix bundle.

